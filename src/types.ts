export enum MovingDirection {
    Up = 'up',
    Down = 'down',
    Left = 'left',
    Right = 'right'
}

export enum AnimationSpeed {  // speed in ms
    Default = 1000 * 0.5,
    Fast = 1000 * 0.1
}